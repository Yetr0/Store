<UserControl x:Class="Store.SubViews.ActionMovies"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Store.SubViews"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" x:Name="title" Text="Action Movies" Margin="10, 0, 0, 0"/>
        <StackPanel Grid.Row="1" Name="StackPanel" Orientation="Horizontal" CanHorizontallyScroll="True" CanVerticallyScroll="False">
            <Grid x:Name="TheGrid1">

            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
