<Window x:Class="Store.RentedMoviesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sub="clr-namespace:Store.SubViews"
        xmlns:local="clr-namespace:Store"
        mc:Ignorable="d"
        Title="Rented Movies" Height="450" Width="800">
    <ScrollViewer>
        <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="20" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <sub:Navigation x:Name="Navigation" Visibility="Visible"></sub:Navigation>
                </Grid>

                <Grid Name="MovieGrid" Grid.Row="1">
                    <sub:Home x:Name="Home" Visibility="Visible"></sub:Home>
                </Grid>

            </Grid>
    </ScrollViewer>
</Window>
